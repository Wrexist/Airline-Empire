# Current Airline Empire workspace

Use **`C:\Users\IsacC\Airline-Empire`** for current development.

The current project includes the latest GitHub Pages website and cinematic store
artwork at `c6ecd07`, with the native clay/glass refresh being developed on
`codex/clay-glass-game-2026-09-14`.

On 14 September 2026, the ten sibling Airline-Empire folders were consolidated
under `build/workspace-archive/2026-09-14/`. This local archive preserves the old
Git worktrees, their branches, uncommitted files, Airport Lab planning and release
artifacts. `manifest.json` records each original location, destination, commit and
uncommitted status. Git worktree pointers were updated by `git worktree move`.

The archive is historical reference, not another current source tree. It is
ignored by Git and is **not backed up by pushing this repository**. No old branch
or unique uncommitted work was deleted. The separate Apple audit branch has
historical diagnostic commits; it was preserved rather than merged over newer
release tooling. Existing unfinished launch-video files in the main workspace
remain untouched.

The most recent verified TestFlight release before this UI refresh is
**1.0.21 (10)**. A newer source checkout is not itself a new TestFlight build.
